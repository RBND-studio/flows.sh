import { FloatingBar } from "@/components/ui/floating-bar";

export default function Home() {
  return (
    <div>
      {/* Check `src/components/feature-announcement.tsx` to see how the announcement component works */}
      <FloatingBar />
    </div>
  );
}
