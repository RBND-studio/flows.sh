import { Columns } from "@/components/columns";

export default function Home() {
  return (
    <>
      {/* To see how the SidebarBanner component is inserted into the page, check out the sidebar.tsx file */}
      <Columns />
    </>
  );
}
