import { type MetaType } from "../../lib/types";

const meta: MetaType = {
  index: "Users",
  properties: "User Properties",
  log: "Event Log",
  "workflow-states": "Workflow States",
};

export default meta;
