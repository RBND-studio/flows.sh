import { type MetaType } from "../../lib/types";

const meta: MetaType = {
  index: "Workflows",
  "adding-blocks": "Adding blocks",
  "configuring-blocks": "Configuring blocks",
};

export default meta;
